<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.jiyun.mapper.BookMapper">

    <select id="getTableDatepa" resultType="cn.jiyun.pojo.Book">
        select tb.*,tp.name publishing
        from db_user_book_borrow.tb_book tb left join tb_publishing tp on tb.pid = tp.id
        <where>
            and tb.del = 0
            <if test="name!=null and name!=''">
                and tb.name like concat('%',#{name},'%')
            </if>
            <if test="author!=null and author!=''">
                and tb.author like concat('%',#{author},'%')
            </if>
            <if test=" publishing!=null and publishing!='' ">
                and tp.name like concat('%',#{publishing},'%')
            </if>
        </where>
    </select>
</mapper>